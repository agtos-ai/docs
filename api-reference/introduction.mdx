---
title: "API Overview"
description: "agtOS exposes three protocol interfaces"
---

# API Reference

agtOS provides three ways to interact with the platform:

| Protocol | Port | Use Case |
|----------|------|----------|
| **REST API** | 4102 | Dashboard, CLI, health checks, chat |
| **WebSocket** | 3000 | Real-time voice audio streaming |
| **MCP** | 4100 | Tool integration for AI agents |

## Authentication

API endpoints support optional authentication via `AGTOS_API_KEY`:

```bash
curl -H "Authorization: Bearer your-api-key" http://localhost:4102/api/health
```

When `AGTOS_API_KEY` is not set, all endpoints are open (local development mode).

<CardGroup cols={3}>
  <Card title="REST Endpoints" icon="server" href="/api-reference/http-endpoints">
    30+ HTTP endpoints for health, chat, sessions, memory, scheduling, devices, config
  </Card>
  <Card title="WebSocket" icon="wave-sine" href="/api-reference/websocket">
    Real-time audio streaming with push-to-talk and VAD
  </Card>
  <Card title="MCP Tools" icon="wrench" href="/api-reference/mcp-tools">
    9 tools: voice, system health, sessions, workflows, scheduling
  </Card>
</CardGroup>
